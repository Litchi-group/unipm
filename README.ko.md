# unipm

> Windows, macOS, Linux를 아우르는 범용 패키지 매니저 오케스트레이터

**unipm**은 서로 다른 운영체제의 패키지 매니저를 하나로 묶어  
하나의 선언 파일로 개발 환경을 구성할 수 있게 해주는 CLI 도구입니다.

---

## ✨ unipm이 필요한 이유

멀티 OS를 사용하는 개발자라면 이런 문제를 겪어본 적 있을 겁니다.

- OS마다 다른 패키지 매니저 (brew, winget, apt 등)
- 같은 도구인데 패키지 이름은 전부 다름
- 새 노트북 세팅 시 반복 작업
- 개발 환경을 코드로 관리하기 어려움

**unipm은 이를 '대체'가 아니라 '통합'으로 해결합니다.**

---

## 🧠 핵심 개념

- unipm은 기존 패키지 매니저를 대체하지 않습니다
- 각 OS의 패키지 매니저를 오케스트레이션합니다
- 하나의 논리 패키지 이름을 OS별 설치 방식으로 매핑합니다

```
vscode
 ├─ macOS   → brew install --cask visual-studio-code
 ├─ Windows → winget install Microsoft.VisualStudioCode
 └─ Linux   → snap install code --classic
```

---

## 🚀 주요 기능 (v0.1)

- Windows / macOS / Linux 지원
- YAML 기반 선언적 설정
- 설치 계획 미리보기
- 선택적 실제 설치 실행
- 서버/데몬/런타임 의존성 없음
- 단일 실행 파일 배포

---

## 📦 설치

> GitHub Releases를 통해 제공 예정

```bash
# 예시 (macOS / Linux)
curl -L https://github.com/yourname/unipm/releases/latest/download/unipm \
  -o /usr/local/bin/unipm
chmod +x /usr/local/bin/unipm
```

---

## 🛠 사용 방법

### 1. 설정 파일 생성
```bash
unipm init
```

---

### 2. 설치 계획 확인
```bash
unipm plan
```

---

### 3. 실제 설치 실행
```bash
unipm apply
```

---

### 4. 환경 점검
```bash
unipm doctor
```

---

## 📜 라이선스

MIT License
